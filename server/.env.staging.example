# Server staging environment
NODE_ENV=production
PORT=5001
CLIENT_URL=https://staging.slaytim.com
TRUST_PROXY=1
BLOCKED_IPS=

JWT_SECRET=change_me_to_very_long_random_secret

AUTH_COOKIE_NAME=slaytim_auth
CSRF_COOKIE_NAME=slaytim_csrf
AUTH_COOKIE_DOMAIN=staging.slaytim.com
AUTH_COOKIE_SAME_SITE=lax
AUTH_COOKIE_SECURE=true

# Runtime: Supabase Transaction Pooler (IPv4 compatible, pgbouncer)
# SECURITY: Supabase Data API/JS client key'leri (SUPABASE_URL, SUPABASE_ANON_KEY,
# SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_*) bu projede kullanılmaz.
DATABASE_URL=postgresql://postgres.jzofovpvgenicknaqfim:<SUPABASE_DB_PASSWORD>@aws-0-eu-west-1.pooler.supabase.com:6543/postgres?schema=public&pgbouncer=true&sslmode=require
# Prisma migrate/introspection: prefer direct URL if network supports IPv6.
# If direct URL is unreachable from your environment, use Session Pooler (5432) as fallback.
DIRECT_URL=postgresql://postgres.jzofovpvgenicknaqfim:<SUPABASE_DB_PASSWORD>@aws-0-eu-west-1.pooler.supabase.com:5432/postgres?schema=public&pgbouncer=true&sslmode=require
SQLITE_MIGRATION_URL=file:./prisma/dev.db

SENTRY_DSN=

REDIS_ENABLED=true
REDIS_URL=redis://127.0.0.1:6379
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

CONVERSION_QUEUE_NAME=slide-conversion
CONVERSION_ATTEMPTS=5
CONVERSION_BACKOFF_MS=5000
CONVERSION_WORKER_CONCURRENCY=2
CONVERSION_LOCK_DURATION_MS=120000
CONVERSION_LOCAL_FALLBACK=false

# Optional hardened mode: run LibreOffice inside Docker sandbox container
# Enable only when API runs on host with Docker access.
CONVERSION_SANDBOX_DOCKER=false
CONVERSION_SANDBOX_IMAGE=lscr.io/linuxserver/libreoffice:latest
CONVERSION_SANDBOX_SOFFICE=/usr/bin/soffice

# Storage (production should be remote)
STORAGE_DRIVER=s3
ALLOW_LOCAL_STORAGE_DEV=false
STORAGE_BUCKET=
STORAGE_REGION=auto
STORAGE_ENDPOINT=
STORAGE_ACCESS_KEY_ID=
STORAGE_SECRET_ACCESS_KEY=
STORAGE_SIGNED_URL_TTL_SECONDS=3600

# ClamAV
CLAMAV_REQUIRED=true
CLAMSCAN_PATH=
